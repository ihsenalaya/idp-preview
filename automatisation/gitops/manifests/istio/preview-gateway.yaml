apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: preview-gateway
  namespace: istio-system
spec:
  selector:
    istio: ingressgateway
  servers:
  - hosts:
    - '*.preview.ihsenalaya.xyz'
    port:
      name: http
      number: 80
      protocol: HTTP
